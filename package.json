{
  "name": "fynder",
  "version": "0.0.1",
  "description": "Fynder CLI ● A beatiful, simple and minimalist find & replace cli.",
  "license": "MIT",
  "esy": {
    "build": "dune build -p #{self.name}",
    "buildDev":
      "refmterr dune build --promote-install-files --root . --only-package #{self.name}",
    "NOTE": "Optional release Section. Customizes result of `esy release`",
    "release": { "rewritePrefix": true, "bin": [ "Main" ] },
    "buildEnv": { "ODOC_SYNTAX": "re" }
  },
  "scripts": {
    "replace": "esy x Main replace",
    "finder": "esy x Main finder",
    "format": "esy dune build @fmt --auto-promote",
    "doc": "esy dune build @doc"
  },
  "dependencies": {
    "@opam/cmdliner": "*",
    "@opam/core": "*",
    "@opam/re": "*",
    "refmterr": "*",
    "@reason-native/console": "*",
    "@reason-native/pastel": "*",
    "@esy-ocaml/reason": ">= 3.6.0 < 4.0.0",
    "@opam/dune": ">=2.6",
    "@opam/inquire": "0.2.1",
    "@opam/lwt": "5.3.0",
    "ocaml": "~4.10.0"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "@reason-native/rely": "*",
    "@opam/odoc": "*",
    "@opam/ocaml-lsp-server": "ocaml/ocaml-lsp:ocaml-lsp-server.opam"
  }
}
